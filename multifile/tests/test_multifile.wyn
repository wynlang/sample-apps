test "basic math" {
    assert_eq(2 + 3, 5)
    assert_eq(10 * 10, 100)
}

test "string operations" {
    assert_eq("hello".upper(), "HELLO")
    assert_eq("WORLD".lower(), "world")
    assert_eq("hello world".contains("world"), 1)
}
