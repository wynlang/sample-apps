// File utilities module
pub fn read_file_safe(path: string) -> string {
    var content = File::read(path);
    if content.len() == 0 {
        return "";
    }
    return content;
}

pub fn write_file_safe(path: string, content: string) -> int {
    File::write(path, content);
    return 1;
}
