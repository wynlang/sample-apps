# Advanced v1.5.0 Example (Work in Progress)

This example demonstrates a more complex real-world application using v1.5.0 features.

**Note:** This example is currently blocked by a type system limitation where HashMap parameters in function signatures cause type inference issues. This will be fixed in v1.5.1.

For a working demonstration of all v1.5.0 features, see `../showcase-v1.5/` instead.

## Planned Features

- User management system with validation
- Multiple enum types for different states
- Pattern matching for business logic
- HashMap-based data storage
- Comprehensive error handling

## Status

⏳ Waiting for HashMap parameter type inference fix in v1.5.1
