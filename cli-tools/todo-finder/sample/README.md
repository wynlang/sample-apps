# sample

A tiny, deliberately debt-laden source tree used as the default scan target for
todo-finder. Its markers are real comment markers, not string literals.

TODO: this line is prose, not a comment marker, so the scanner ignores it.
