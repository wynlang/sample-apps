# Widget API

A deliberately imperfect document, so `md-toc` has something to find.

## Getting Started

Install it, then check the version:

```bash
# Install the CLI -- this is a comment, NOT a heading.
npm install -g widget

# Verify
widget --version
```

#### Requirements

That jumped from H2 straight to H4 -- one level skipped.

## Usage

### Basic Usage

Pass a widget name.

### Basic Usage

The same heading twice: both anchors are `#basic-usage`, so the second link is dead.

## FAQ / Troubleshooting 🚀

Punctuation and emoji are stripped from the anchor, exactly as GitHub does it.

# Appendix

A second H1 -- a document should have one title.
